
import { arrReverse } from 'safety-net/helpers/arr-reverse';
import { module, test } from 'qunit';

module('Unit | Helper | arr reverse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrReverse([42]);
  assert.ok(result);
});

